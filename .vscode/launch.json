{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FCS: Debug",
      "type": "dotnet",
      "request": "launch",
      "projectPath": "${workspaceFolder}/src/FCS.csproj",
      "preLaunchTask": "clean and build"
    }
  ]
}
